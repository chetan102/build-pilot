# Infrastructure Directory

Contains Docker Compose definitions, container Dockerfiles, and Caddy reverse proxy configurations.

- `docker/`: Dockerfiles for services and sandbox runner.
- `caddy/`: Caddyfile and TLS routing setup.
- `docker-compose.yml`: Multi-container local environment.

